# Configuração de Variáveis de Ambiente
# Copie este arquivo para .env.local para desenvolvimento local
# Configure essas variáveis na Vercel para produção

# JWT Secret para autenticação (obrigatório)
JWT_SECRET=sua_chave_secreta_jwt_aqui

# Token de API do Mantis (obrigatório)
MANTIS_API_TOKEN=seu_token_aqui

# URL base do Mantis (opcional - usa valor padrão se não definido)
MANTIS_BASE_URL=https://mantis.xcelis.com.br/mantis

# PostgreSQL para Monitor de Jobs (obrigatório para o monitor)
# IMPORTANTE: Esta é uma conexão SEPARADA da POSTGRES_URL do sistema principal
JOBS_POSTGRES_URL=postgresql://usuario:senha@host:porta/database

# Modo de debug (opcional - false por padrão)
DEBUG_MODE=false
